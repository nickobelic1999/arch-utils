#Chroot into container
sudo arch-chroot $1 /bin/zsh << "EOT"

#Add user admin and set initial password to 'admin'
useradd -m -G wheel,storage,power,users,input -s /bin/zsh $2
echo -e "$2\n$2" | passwd $2

#Make user admin a sudoer
echo $2'  ALL=(ALL:ALL) ALL' >> /etc/sudoers

EOT
